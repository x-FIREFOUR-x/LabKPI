#include <vector>
#include "Ad.h"
#include "User.h"

void readUsers(std::vector<User>&);
void writeUsers(std::vector<User>&);
void readAds(std::vector<Ad>&);
void writeAds(std::vector<Ad>&);
vector<string> createPhoneNumbers(vector<User>);